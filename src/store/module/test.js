const state = {
  demo: 0
}

const mutations = {
  changeDemo(state, val){
    state.demo = val
  }
}

const actions = {
  
}

const getters = {
  
}

export default {
  state,
  mutations,
  actions,
  getters,
}
